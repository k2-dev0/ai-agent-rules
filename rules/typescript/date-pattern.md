# 日付処理の編集ルール

## import と既存処理

1. 既存の日付処理と対象packageのalias・依存関係を確認する。`@/modules/dayjs`などの共通入口が実在し、同じ契約を扱える場合はそれを使う。
2. 共通入口が非推奨なら、プロジェクトで使われている代替へ合わせる。存在しないmodule、wrapper、別ライブラリをこの規約だけのために追加しない。
3. 日付と日時、timezone、境界の包含条件は呼び出し元の契約に合わせる。
