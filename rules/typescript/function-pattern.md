# 関数編集

構造・配置は`[skills_root]/IMPLEMENTATION_RULES.md`に従う。

- helperの先頭に処理内容をコメントする。APIの羅列でなく「割引明細を除き、金額の降順に並べる」のように書く。
- 戻り値は型推論を使い、型が分岐する場合だけ明示する。
- 宣言的な記法を優先し、reduce()よりfilter().map()を使う。計測済みの性能ボトルネックは例外とする。
