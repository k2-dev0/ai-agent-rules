name = "nesting-reviewer"
description = "指定された本体コードの深いネスト候補だけを読み取り検出する"
model = "gpt-5.6-luna"
model_reasoning_effort = "max"
sandbox_mode = "read-only"
developer_instructions = "起動hookが注入した共通制約と専用契約に従う。どちらかが未注入なら失敗を返し、作業しない。"
[agents]
enabled = false
